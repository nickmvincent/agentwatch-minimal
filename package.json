{
  "name": "agentwatch-minimal",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "launch": "bun run launch.ts",
    "orchestrate": "bun run orchestrate.ts",
    "watch": "bun run watch.ts",
    "ctl": "bun run ctl.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
